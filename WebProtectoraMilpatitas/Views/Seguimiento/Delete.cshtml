@model WebProtectoraMilpatitas.Models.SeguimientoViewModel

<h2>Eliminar Seguimiento</h2>
<div class="formEliminarGuapo">
    <h2>¿Estás seguro de que quieres eliminar el seguimiento @ViewData["IdSeg"] de @ViewData["NombreSeg"]?</h2>
</div>
<div>
    <hr />

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color parrafoCentrar">
            <input type="submit" value="Eliminar" class="btn btn-default" /> |
            <i class="fas fa-eye"></i> @Html.ActionLink("Volver a la lista", "Index")
        </div>
    }
</div>
